"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
import AppShell from "@/components/AppShell";
import Notes from "@/components/Notes";
import SecurityActivity from "@/components/SecurityActivity";
export default function DashboardPage(){const router=useRouter();useEffect(()=>{if(!localStorage.getItem("token"))router.push("/login");},[router]);return <AppShell><header className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><span className="eyebrow">Secure workspace</span><h1 className="mt-2 text-3xl font-bold tracking-tight text-gray-900">Welcome back</h1><p className="mt-2 text-sm leading-6 text-gray-600">Your secure workspace is active and protected.</p></div><a href="#note-form" className="btn-primary w-fit">Create encrypted note</a></header><section aria-label="Security overview" className="mt-8 grid gap-4 md:grid-cols-3">{[["Encryption","AES-256-GCM","Your notes remain encrypted at rest."],["Access","MFA protected","A second factor protects every sign-in."],["Storage","Private workspace","Only authenticated requests can access notes."]].map(([label,value,copy])=><article key={label} className="surface p-5"><div className="flex size-10 items-center justify-center rounded-xl bg-[#FFF1E6] text-orange-600"><span className="size-2.5 rounded-full bg-orange-500" /></div><p className="mt-4 text-xs font-semibold uppercase tracking-wider text-gray-500">{label}</p><h2 className="mt-1 text-lg font-bold text-gray-900">{value}</h2><p className="mt-2 text-sm leading-6 text-gray-500">{copy}</p></article>)}</section><div className="mt-8"><Notes /></div><SecurityActivity /></AppShell>;}
